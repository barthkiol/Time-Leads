<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.timeleads">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TimeLeads">
        <activity
            android:name="TimeLeads.example.timeleads.StudentsActivity"
            android:exported="false" />
        <activity
            android:name="TimeLeads.example.timeleads.MainActivityCoordenador"
            android:exported="false" />

        <activity
            android:name="TimeLeads.example.timeleads.CriarChamado"
            android:exported="false" />
        <activity
            android:name="TimeLeads.example.timeleads.CriarAluno"
            android:exported="true" />
        <activity
            android:name="TimeLeads.example.timeleads.LoginActivity"
            android:exported="true">

        </activity>
        <activity
            android:name="TimeLeads.example.timeleads.SalvarEvento"
            android:exported="true" />
        <activity
            android:name="TimeLeads.example.timeleads.RequestActivity"
            android:exported="false" />
        <activity
            android:name="TimeLeads.example.timeleads.EventActivity"
            android:exported="true" />
        <activity android:name="TimeLeads.example.timeleads.MainActivity" />
        <activity
            android:name="TimeLeads.example.timeleads.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>